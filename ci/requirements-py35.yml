name: test_env
dependencies:
    - python=3.5
    - dask
    - h5py
    - netcdf4
    - numpy
    - pytest
    - pyyaml
    - xarray
    - pip:
        - coveralls
        - pytest-cov
        - h5netcdf
